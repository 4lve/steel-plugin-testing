[package]
name = "plugin-announcer"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
stabby = { workspace = true, features = ["std"] }
steel-api = { path = "../steel-api" }
