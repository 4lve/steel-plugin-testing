[package]
name = "host"
version = "0.1.0"
edition = "2021"

[dependencies]
stabby = { workspace = true, features = ["std", "libloading"] }
steel-api = { path = "../steel-api" }
plugin-announcer = { path = "../plugin-announcer" }
