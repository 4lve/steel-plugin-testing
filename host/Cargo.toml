[package]
name = "host"
version = "0.1.0"
edition = "2021"

[dependencies]
libloading = ">=0.7.3, <0.10"
stabby = { workspace = true, features = ["std", "libloading"] }
steel-api = { path = "../steel-api" }
